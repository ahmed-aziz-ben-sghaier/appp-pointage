require('./bootstrap');
$(document).ready(function () {
    // Activer les modales Bootstrap
    var presenceModal = new bootstrap.Modal(document.getElementById('presenceModal'));
    var delayRequestModal = new bootstrap.Modal(document.getElementById('delayRequestModal'));
    
});
